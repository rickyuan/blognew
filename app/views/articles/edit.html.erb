<div class="container">
	<div id="content">
	<div class="row">
		<div class="span9">
			<span class="page_title">修改文章</span>
	 		<%= form_for([@category, @article]) do |f| %>
				<div>
					<%= f.label :title, '标题' %>
					<%= f.text_field :title %>
				</div>
				<div>
					<%= f.label :content, '内容' %>
					<%= f.text_area(:content, :rows => 10, :class => 'span9') %>
				</div>
				<div>
					<%= f.label :category_id, '类别' %>
					<%= f.collection_select :category_id, @categories, :id, :name, {:prompt => false} %>
				</div>
				<div>
					<%= f.submit '更新' %>
				</div>
			<% end %>
		</div>
	</div>
	</div>
</div>